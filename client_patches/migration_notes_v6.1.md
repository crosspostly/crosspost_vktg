# 📋 МИГРАЦИОННЫЕ ЗАМЕТКИ CLIENT.GS v6.1

**Дата**: 2025-11-04  
**Ветка**: feature/client-license-cache-and-publish-fixes  
**Цель**: Критические исправления лицензионного кеша, листов с именами групп, template literals

---

## 🚨 **НАЙДЕННЫЕ КРИТИЧНЫЕ ПРОБЛЕМЫ В ТЕКУЩЕМ КОДЕ:**

### **1️⃣ Template Literals (БЛОКИРУЮТ РАБОТУ!)**
```javascript
// ❌ НАШЕЛ В client.gs строка ~627:
`Binding ID: ${bindingId}, Name: ${bindingName}`

// ✅ ЗАМЕНИТЬ НА:
"Binding ID: " + bindingId + ", Name: " + bindingName
```
**КРИТИЧНО:** Google Apps Script не поддерживает ES6 template literals!

### **2️⃣ Дублирующие константы лицензии**
```javascript
// ❌ НАШЕЛ В client.gs строка ~25:
var LICENSE_CACHE_TTL_MS = 30 * 60 * 1000; // 30 минут
var USER_PROP_LICENSE_KEY = 'LICENSE_KEY';
var USER_PROP_LICENSE_META = 'LICENSE_META';
```
**ДЕЙСТВИЕ:** Заменить значение TTL на 24 часа: `24 * 60 * 60 * 1000`

### **3️⃣ Функция getLicense() уже существует, но БЕЗ кеша**
**ДЕЙСТВИЕ:** НЕ удалять, а ЗАМЕНИТЬ на getLicenseCached() полностью

### **4️⃣ cleanupOrphanedCache() существует и вызывается**
**ДЕЙСТВИЕ:** Найти ВСЕ вызовы и удалить, саму функцию удалить

---

## 📝 **ПОШАГОВЫЙ ПЛАН МИГРАЦИИ:**

### **Коммит 1: Исправление template literals (КРИТИЧНО!)**
- Найти ВСЕ template literals в строке ~627 и других местах
- Заменить на классическую конкатенацию
- Проверить HTML template (там допускаются)

### **Коммит 2: Обновление констант лицензионного кеша**
- Изменить LICENSE_CACHE_TTL_MS с 30 минут на 24 часа
- Оставить USER_PROP_LICENSE_KEY и USER_PROP_LICENSE_META

### **Коммит 3: Замена функции getLicense() на getLicenseCached()**
- ПОЛНОСТЬЮ заменить текущую функцию getLicense() на новую getLicenseCached()
- Старую функцию сохранить в functions_removed_v6.1.js для истории

### **Коммит 4: Замена всех вызовов getLicense() → getLicenseCached(false)**
- getInitialData: строка ~89
- addBinding: строка ~149
- editBinding: строка ~190
- deleteBinding: строка ~261
- toggleBindingStatus: строка ~303
- publishLastPost: строка ~331
- setGlobalSetting: строка ~390
- getGlobalSetting: строка ~422
- getBindings: строка ~465
- checkNewPosts: строка ~540
- sendPostToServer: строка ~650
- getVkPosts: строка ~756

### **Коммит 5: Обновление saveLicenseWithCheck() и changeLicense()**
- saveLicenseWithCheck(): добавить getLicenseCached(true) после успеха
- changeLicense(): добавить очистку кеша UserProperties + appState.license

### **Коммит 6: Удаление cleanupOrphanedCache()**
- Функция cleanupOrphanedCache() полностью удаляется
- Удалить вызовы в addBinding строка ~218 и editBinding строка ~236
- Удалить из HTML интерфейса если есть упоминания

### **Коммит 7: Функции работы с листами - имена групп**
- extractSheetNameFromVkUrl(): новая функция
- isPostAlreadySent(): заменить на версию с sheetName
- markPostAsSent(): заменить на версию с sheetName  
- ensureAllPublishedSheetsExist(): заменить на версию с именами групп
- getOrCreatePublishedPostsSheet(): обновить логику именования

### **Коммит 8: Добавление processVkLinks() для гиперссылок**
- Новая функция processVkLinks() в раздел утилит
- Интеграция в форматирование постов (если есть в client.gs)

### **Коммит 9: Безопасная публикация publishLastPost()**
- Заменить текущий publishLastPost() на версию с safePickLastPost()
- Добавить функцию safePickLastPost() с полными проверками

### **Коммит 10: Удаление Properties-кешей постов**
- Найти и удалить любые функции с PropertiesService для lastPostIds
- Поиск по ключевым словам: "lastPostIds_", "getScriptProperties" + "lastPost"
- Заменить на работу только с листами

---

## 🔍 **КОНКРЕТНЫЕ МЕСТА В КОДЕ (НАЙДЕНО В АНАЛИЗЕ):**

### **Template Literals (НАЙДЕНО):**
```javascript
// Строка ~627 в HTML template:
\`Binding ID: \${bindingId}, Name: \${bindingName}\`
```
**ИСПРАВЛЕНИЕ:** Это ВНУТРИ HTML template string, поэтому ДОПУСТИМО. НО проверить на всякий случай другие места.

### **LICENSE_CACHE_TTL_MS (НАЙДЕНО):**
```javascript
// Строка ~25:
var LICENSE_CACHE_TTL_MS = 30 * 60 * 1000; // 30 минут
```
**ИСПРАВЛЕНИЕ:** Заменить на `24 * 60 * 60 * 1000; // 24 часа`

### **getLicense() (НАЙДЕНО):**
```javascript
// Строки ~457-512 - функция getLicense() существует
// НО она делает запрос к серверу КАЖДЫЙ РАЗ!
```
**ИСПРАВЛЕНИЕ:** ПОЛНОСТЬЮ заменить на getLicenseCached()

### **cleanupOrphanedCache() вызовы (НАЙДЕНО):**
```javascript
// Строка ~218 в addBinding():
const cleanupResult = cleanupOrphanedCache();

// Строка ~236 в editBinding():
const sheetsResult = ensureAllPublishedSheetsExist();
```
**ИСПРАВЛЕНИЕ:** Удалить оба вызова cleanupOrphanedCache()

### **Published листы (АНАЛИЗ):**
```javascript
// getOrCreatePublishedPostsSheet() использует bindingName для имени:
const safeName = bindingName.replace(/[^\w\s\-_а-яА-ЯёЁ]/g, '')
sheetName = `Published_${safeName}`;
```
**ПРОБЛЕМА:** Это близко к требованию, но нужно убрать префикс Published_
**ИСПРАВЛЕНИЕ:** Использовать только имя группы без префикса

---

## ⚡ **ПОРЯДОК ВЫПОЛНЕНИЯ (ПРИОРИТЕТ):**

1. **🔴 БЛОКИРУЮЩИЕ** (в первую очередь):
   - Template literals исправление
   - TTL лицензии 24 часа
   - getLicense() → getLicenseCached() замена

2. **🟡 ВЫСОКИЕ**:
   - Удаление cleanupOrphanedCache()
   - Листы с именами групп
   - publishLastPost() защита от undefined

3. **🟢 УЛУЧШЕНИЯ**:
   - processVkLinks() для гиперссылок
   - Удаление Properties-кешей постов
   - extractSheetNameFromVkUrl()

---

## ✅ **ГОТОВЫЕ ФАЙЛЫ ДЛЯ КОПИПАСТЫ:**
- `functions_added_v6.1.js` - новые функции (ГОТОВ)
- `functions_removed_v6.1.js` - функции для удаления (ГОТОВ)
- `TODO_v6.1_CRITICAL.md` - полный план (ГОТОВ)

**СЛЕДУЮЩИЙ ШАГ:** Начать пофункциональное внедрение по коммитам 1-10.