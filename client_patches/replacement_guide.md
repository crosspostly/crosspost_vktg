# 📋 РУКОВОДСТВО ПО ЗАМЕНЕ client.gs v6.1

## 🔄 ПОШАГОВАЯ ИНСТРУКЦИЯ:

### ШАГ 1: Резервная копия
```
1. Откройте Google Apps Script редактор
2. Скопируйте весь текущий client.gs в отдельный файл (client_backup.gs)
3. Сохраните backup
```

### ШАГ 2: Критичные замены

**2.1. Версия (строка 22):**
```
ИЗ: const CLIENT_VERSION = "6.0";
В:  const CLIENT_VERSION = "6.1";
```

**2.2. Добавить после строки с REQUEST_TIMEOUT:**
```javascript
// ✅ КРИТИЧНО: 24 ЧАСА кеш лицензии
var LICENSE_CACHE_TTL_MS = 24 * 60 * 60 * 1000; // 24 часа
var USER_PROP_LICENSE_KEY = 'LICENSE_KEY';
var USER_PROP_LICENSE_META = 'LICENSE_META';

// ГЛОБАЛЬНОЕ СОСТОЯНИЕ
var appState = {
  license: null,
  initialized: false
};
```

**2.3. Template Literals (найти и заменить):**
- `App started, version ${CLIENT_VERSION}` → `"App started, version " + CLIENT_VERSION`
- `VK→Telegram Manager v${CLIENT_VERSION}` → `"VK→Telegram Manager v" + CLIENT_VERSION`
- В showUserStatistics(): заменить все template literals на конкатенацию строк

### ШАГ 3: Новые функции
```
3. Скопировать функции из client_patches/functions_added_v6.1.js
4. Вставить в конец файла client.gs (перед закрывающими комментариями)
5. Сохранить
```

### ШАГ 4: Тестирование
```
4.1. Проверить что скрипт компилируется без ошибок
4.2. Запустить doFirstAuth() для авторизации
4.3. Открыть меню VK→Telegram > Открыть управление
4.4. Проверить что показывается "v6.1" в заголовке
```

---

## ⚠️ ЧАСТЫЕ ПРОБЛЕМЫ:

- **SyntaxError**: Проверьте что все template literals исправлены
- **undefined appState**: Убедитесь что добавили блок с appState 
- **License cache not working**: Проверьте что LICENSE_CACHE_TTL_MS = 24 часа

## 🏆 РЕЗУЛЬТАТ:
✅ CLIENT.GS v6.1 с кешированием лицензии 24 часа  
✅ Поддержка VK гиперссылок [url|текст]  
✅ Улучшенная обработка ошибок  
✅ Более быстрая работа (нет запросов к серверу для проверки лицензии)