# 📋 План улучшений VK→Telegram Crossposter

**Дата:** 2025-11-05  
**Версия:** v1.0  
**Статус:** Готов к реализации

---

## 🎯 Обзор текущего состояния

### ✅ Что работает хорошо
- **Клиентская часть (client.gs)**: Полностью функциональна
  - VK URL → ID extraction с валидацией
  - UI/UX улучшения (collapse/expand, прогресс-индикаторы)
  - Published листы с интеллектуальным именованием
  - Асинхронная загрузка названий VK/TG
  - Детальное логирование операций

- **Архитектурная основа**: Чётко определена
  - Централизованный сервер с Web App
  - Лицензионная система контроля доступа
  - Published листы как источник правды
  - Разделение секретов между сервером и клиентом

### 🚨 Критические проблемы
1. **Серверное отставание**: Клиент готов, сервер не синхронизирован
2. **VK API интеграция**: Отсутствует поддержка `vk_group_id` в `get_vk_posts`
3. **Медиа обработка**: VK видео отправляются как ссылки вместо файлов
4. **Миграция данных**: Старые Published листы не обновлены
5. **API неполнота**: Отсутствует `publish_last_post` endpoint

---

## 🔍 Анализ несоответствий документации и кода

### 📋 UNIFIED_TODO.md vs Реальность
| Задача | Документация | Фактический статус | Проблема |
|--------|-------------|-------------------|----------|
| `get_vk_posts` по `vk_group_id` | Требуется реализация | Отсутствует в server.gs | Критично |
| Миграция Bindings с новыми полями | В работе | Не реализовано | Высокий |
| `publish_last_post` | Планируется | Отсутствует endpoint | Средний |
| VK видео прямые файлы | Архитектура описана | Не реализовано | Высокий |

### 🏗️ ARCHITECTURE.md vs Реальность
| Принцип | Документация | Реализация | Статус |
|---------|-------------|------------|--------|
| Published листы как источник правды | ✅ | ✅ | Соответствует |
| Запрет Properties кеша | ✅ | ⚠️ (части в client.gs) | Требует проверки |
| VK видео через video.get | ✅ | ❌ | Не реализовано |
| Медиагруппы оптимизация | ✅ | ⚠️ (базовая) | Требует доработки |

---

## 🚀 Детальный план улучшений по категориям

## 🔥 HIGH PRIORITY (Критичные баги и функционал)

### 1. Синхронизация сервера с клиентом по VK Group ID
**Проблема:** Клиент передает `vk_group_id`, сервер ожидает URL  
**Файлы:** `server/api_endpoints.gs`, `server.gs`  
**Объём:** 4-6 часов  

**Конкретные изменения:**
```javascript
// server/api_endpoints.gs - обновить handleGetVkPosts
function handleGetVkPosts(payload, clientIp) {
  const { license_key, vk_group_id, count = 50 } = payload;
  
  // Валидация формата
  if (!/^-?\d+$/.test(vk_group_id)) {
    return jsonResponse({ success: false, error: "Invalid vk_group_id format" }, 400);
  }
  
  // Логирование запроса
  const logUrl = `https://api.vk.com/method/wall.get?owner_id=${vk_group_id}&count=${count}&v=${VK_API_VERSION}&access_token=[HIDDEN]`;
  logEvent("DEBUG", "vk_api_request", license_key, `Request: ${logUrl}, IP: ${clientIp}`);
  
  // Вызов VK API с owner_id
  // ...
}
```

### 2. Миграция Bindings с новыми полями
**Проблема:** Отсутствуют `Binding Name`, `Binding Description`  
**Файлы:** `server.gs` (initializeServer, handleAddBinding, handleEditBinding)  
**Объём:** 3-4 часа  

**Конкретные изменения:**
- Обновить структуру листа `Bindings`
- Добавить автосоздание колонок при первом запуске
- Обновить все CRUD операции для Bindings
- Реализовать `getUserBindingsWithNames()` с новыми полями

### 3. Прямое скачивание VK видео
**Проблема:** Видео отправляются как embed ссылки вместо файлов  
**Файлы:** `server.gs` (getVkVideoDirectUrl, sendTelegramVideo)  
**Объём:** 4-5 часов  

**Конкретные изменения:**
```javascript
// Реализация getVkVideoDirectUrl()
function getVkVideoDirectUrl(videoId) {
  const userToken = PropertiesService.getScriptProperties().getProperty("VK_USER_ACCESS_TOKEN");
  const response = UrlFetchApp.fetch(
    `https://api.vk.com/method/video.get?videos=${videoId}&v=5.131&access_token=${userToken}`,
    { muteHttpExceptions: true, timeout: 15000 }
  );
  
  const data = JSON.parse(response.getContentText());
  if (data.response?.items?.[0]?.files) {
    const files = data.response.items[0].files;
    return files.mp4_720 || files.mp4_480 || files.mp4_360 || files.hls;
  }
  return null;
}
```

### 4. Реализация publish_last_post
**Проблема:** Отсутствует endpoint для публикации последнего поста  
**Файлы:** `server/api_endpoints.gs`, `server.gs`  
**Объём:** 2-3 часа  

**Конкретные изменения:**
- Добавить case `publish_last_post` в doPost()
- Реализовать `handlePublishLastPost()` с форматированием
- Интегрировать с format_settings связки

---

## ⚡ MEDIUM PRIORITY (Рефакторинг и производительность)

### 5. Оптимизация медиагрупп Telegram
**Проблема:** Фото отправляются по одному, нет оптимизации смешанного контента  
**Файлы:** `server.gs` (sendMixedMediaOptimized)  
**Объём:** 3-4 часа  

**Стратегия реализации:**
- Фото (1-10): один sendMediaGroup запрос
- Видео (1): один sendVideo запрос  
- Видео (>1): отдельные запросы с caption только у первого
- Фото + видео: sendVideo + sendMediaGroup без caption

### 6. Улучшенная обработка ошибок VK API
**Проблема:** Нет детальной обработки кодов ошибок (5, 10, 15, 200)  
**Файлы:** `server.gs` (VK API вызовы)  
**Объём:** 2-3 часа  

**Конкретные изменения:**
- Логировать коды ошибок и тексты
- Реализовать ретраи для временных ошибок
- Таймауты по типу операции (FAST/MEDIUM/SLOW)

### 7. Миграция старых Published листов
**Проблема:** Листы `Published_-123456` не переименованы в `Published_GroupName`  
**Файлы:** `client.gs` (функция миграции)  
**Объём:** 2 часа  

**Алгоритм миграции:**
1. Найти все листы `Published_-*`
2. Получить binding_name по vk_group_id
3. Переименовать с безопасным именованием
4. Обновить колонку "VK Post URL"

---

## 🔧 LOW PRIORITY (Улучшения и новые фичи)

### 8. Batch запросы названий VK/TG
**Проблема:** Названия запрашиваются по одному, медленно  
**Файлы:** `server.gs` (batch API функции)  
**Объём:** 3-4 часа  

### 9. Расширенное форматирование текста
**Проблема:** Ограниченные опции форматирования  
**Файлы:** `client.gs` (UI), `server.gs` (processing)  
**Объём:** 4-5 часов  

### 10. UI улучшения админ панели
**Проблема:** Базовый интерфейс управления  
**Файлы:** `server.gs` (HTML шаблоны)  
**Объём:** 3-4 часа  

---

## 📅 Roadmap реализации

### 🏃‍♂️ Sprint 1 (Неделя 1) - Критическая синхронизация
**Цель:** Восстановить работоспособность основной функциональности

- [ ] **Понедельник:** `get_vk_posts` по `vk_group_id`
- [ ] **Вторник:** Миграция Bindings с новыми полями  
- [ ] **Среда:** `publish_last_post` endpoint
- [ ] **Четверг:** Тестирование и отладка
- [ ] **Пятница:** Документирование и деплой

### 🚀 Sprint 2 (Неделя 2) - Медиа улучшения
**Цель:** Полноценная работа с медиаконтентом

- [ ] **Понедельник:** Прямое скачивание VK видео
- [ ] **Вторник:** Оптимизация медиагрупп Telegram
- [ ] **Среда:** Улучшенная обработка ошибок VK API
- [ ] **Четверг:** Миграция Published листов
- [ ] **Пятница:** Интеграционное тестирование

### 🔧 Sprint 3 (Неделя 3) - Качество и производительность
**Цель:** Стабильность и оптимизация

- [ ] **Понедельник-Среда:** Batch запросы названий
- [ ] **Четверг:** Расширенное форматирование
- [ ] **Пятница:** UI улучшения и финальное тестирование

---

## 🧪 Рекомендации по тестированию

### 🔍 Критические тесты (High Priority)
1. **VK URL → ID extraction:**
   - `https://vk.com/club96798355?from=groups` → `-96798355`
   - `https://vk.com/public123` → `-123`
   - `https://vk.com/id123` → `123`
   - Число `-12345` → `-12345`

2. **VK видео прямые файлы:**
   - Проверить получение direct URL через video.get
   - Тест отправки в Telegram как файл
   - Fallback на embed при недоступности

3. **Published листы миграция:**
   - Старый лист → новый формат
   - Сохранение данных при переименовании
   - Обновление VK Post URL колонки

### ⚡ Функциональные тесты (Medium Priority)
1. **Медиагруппы оптимизация:**
   - 10 фото → 1 запрос
   - 1 видео + 5 фото → 2 запроса
   - 3 видео → 3 запроса

2. **Обработка ошибок VK API:**
   - Код 5 (неавторизован)
   - Код 10 (internal server error)
   - Код 15 (access denied)
   - Код 200 (ошибка доступа к альбому)

### 🎯 UI/UX тесты (Low Priority)
1. **Batch загрузка названий:**
   - Скорость загрузки 10+ связок
   - Кеширование в браузере
   - Обработка ошибок загрузки

---

## 📊 Мониторинг и метрики

### 🔧 Ключевые метрики производительности
1. **API Response Time:**
   - `get_vk_posts`: < 8 секунд
   - `send_post`: < 15 секунд  
   - `video.get`: < 30 секунд

2. **Success Rates:**
   - VK API calls: > 95%
   - Telegram API calls: > 98%
   - Published листы операции: > 99%

3. **Error Monitoring:**
   - VK API ошибки по кодам
   - Telegram rate limits
   - Published листы конфликты

### 📈 Логирование и алерты
- **Critical:** VK/Telegram API недоступность
- **Warning:** Высокое время ответа (>30 секунд)
- **Info:** Успешные операции с метриками

---

## 🛡️ Рекомендации по безопасности

### 🔐 Валидация входных данных
1. **vk_group_id:** regex `^-?\d+$`
2. **Telegram chat_id:** формат @username или числовой
3. **License keys:** формат и длина проверки
4. **URL параметры:** sanitize и whitelist

### 🛡️ Защита токенов
1. **Server side:** хранить только в ScriptProperties
2. **Client side:** никогда не передавать токены
3. **Logs:** маскировать токены в логах
4. **Rate limiting:** защита от abuse

### 📋 Контроль логов
1. **Rotation:** автоматическая очистка старых логов
2. **PII:** удаление персональных данных из логов
3. **Access control:** ограниченный доступ к логам
4. **Retention:** политика хранения логов

---

## 🎯 Заключение

**Текущее состояние:** Система функциональна на 70% - клиент готов, сервер требует синхронизации.

**Приоритет:** Критические улучшения (Sprint 1-2) для восстановления полной работоспособности.

**Ресурсы:** ~40-50 часов разработки, ~10 часов тестирования.

**Риск:** Низкий - все изменения в рамках существующей архитектуры.

**Результат:** Полностью функциональная, стабильная и производительная система кросспостинга VK→Telegram.

---

*Документ подготовлен на основе анализа UNIFIED_TODO.md, ARCHITECTURE.md, server.gs, client.gs и server/api_endpoints.gs от 2025-11-05.*