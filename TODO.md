# 📋 TODO - VK→Telegram Crossposter

## 🚨 **КРИТИЧЕСКИЕ ОШИБКИ server.gs - НУЖНО ИСПРАВИТЬ НЕМЕДЛЕННО!**

**🔥 Статус**: КОД НЕ МОЖЕТ БЫТЬ ВЫПОЛНЕН - SyntaxError при сохранении в Google Apps Script!

### **❌ Найдено 11 критических ошибок:**

#### **1. 🔄 Дублирующиеся функции (4 случая)**
- **`handleGetBindings()`** - объявлена ДВАЖДЫ (строки ~430 и ~450)
- **`handleSendPost()`** - объявлена ДВАЖДЫ (строки ~640 и ~780)
- **`handleTestPublication()`** - объявлена ДВАЖДЫ (строки ~680 и ~850)
- **`getUserBindingsWithNames()`** - объявлена ДВАЖДЫ (строки ~950 и ~1100)

💥 **Результат**: JavaScript не может выполнить код с дублирующимися объявлениями!

#### **2. 💔 Неполная функция getVkGroupName (строка ~900)**
```javascript
// Код обрывается посреди функции!
if (!userToken) {
  throw new Error("VK User Access Token not configured");
}
// ... код без function declaration!
```

#### **3. 🔗 Отсутствуют ключевые функции**
- **`extractVkGroupId()`** - вызывается везде, но объявлена только в конце
- **`extractTelegramChatId()`** - аналогично
- **`getVkGroupName()`** - вызывается, но не объявлена корректно

### **🔧 ПЛАН ИСПРАВЛЕНИЙ**

#### **Приоритет 1 - КРИТИЧНО (код не запустится)**
1. ✅ **Удалить все дублирующиеся функции** 
   - Оставить только лучшую версию каждой
   - Для `getUserBindingsWithNames()` оставить версию 2 (строки ~1100-1160)

2. ✅ **Исправить неполную функцию getVkGroupName**
   - Удалить оборванный код в строке ~900
   - Полная функция уже есть в строке ~1060

3. ✅ **Переместить объявления функций**
   - `extractVkGroupId()` и `extractTelegramChatId()` переместить в начало
   - Они вызываются до своего объявления

4. ✅ **Унифицировать const/let**
   - Заменить `var` на `const/let` в `getAdminPanelHtml()`
   - Современный JavaScript синтаксис

---

## 🚀 **ОДОБРЕННЫЕ УЛУЧШЕНИЯ**

### **P0 - Критично (сделать в ближайшее время)**

#### 1. ✅ **Retry механизм для API**
- Автоматические повторы при временных ошибках VK/Telegram API
- Exponential backoff (1с, 2с, 4с, 8с)
- Логирование попыток повторов
- Максимум 3-5 попыток на запрос

#### 2. ✅ **Health Check Endpoint**  
- Добавить `/health` endpoint в server.gs
- Проверка работоспособности VK/Telegram API
- Статус системы (JSON ответ)
- Мониторинг доступности сервера извне

### **P1 - Высокий приоритет**

#### 3. ✅ **Проверка дублирования закрепленных постов**
- Убедиться что закрепленные посты ВК не дублируются
- Результаты должны сохраняться в Google Sheet лист с названием сообщества
- Проверить корректность работы `getOrCreatePublishedPostsSheet(vkGroupId)`

#### 4. ✅ **Улучшенная обработка attachment**
- Пропускать неподдерживаемые медиа типы  
- Оставлять только: фото, видео, подкасты
- Игнорировать сложные attachment комбинации
- Логировать пропущенные типы медиа

#### 5. ✅ **Bulk операции для связок**
- Массовое включение/выключение связок
- Выбор нескольких связок чекбоксами  
- Кнопки "Включить выбранные" / "Выключить выбранные"
- Массовое тестирование связок

### **P2 - Средний приоритет** 

#### 6. ✅ **Планировщик постов**
- Возможность отложить публикацию на определенное время
- Очередь постов с планируемым временем отправки
- Интерфейс выбора времени публикации
- Хранение отложенных постов в отдельном листе

### **P3 - Низкий приоритет (далекое будущее)**

#### 7. ✅ **Drag & Drop для связок**
- Перетаскивание связок для изменения порядка
- Сохранение порядка в Google Sheet
- HTML5 drag & drop API в клиентском интерфейсе
- Визуальные индикаторы при перетаскивании

---

## 📝 **ПРИМЕЧАНИЯ**

- Все изменения должны быть обратно совместимы
- Обязательно тестирование перед внедрением  
- Логирование всех новых функций
- Обновление документации при добавлении функций

---

**Последнее обновление**: 01 ноября 2025 (+ server.gs анализ)  
**Статус server.gs**: ❌ Критические ошибки - код не выполнится  
**Статус проекта**: Production-Ready (базовая функциональность после исправлений)