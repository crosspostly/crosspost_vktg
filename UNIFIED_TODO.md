# 📋 Unified TODO — VK→Telegram Crossposter (Server + Client + Fixes + Code Samples)

Обновлено: 2025-11-03
Назначение: ЕДИНЫЙ файл задач. Все прочие todo/readme/to_fix — удалить. Рабочая истина здесь.

---

## 🔧 SERVER (server.gs)

### A. Рефакторинг (от простого к сложному)
1) API-обёртка и JSON парсер
- [ ] makeApiRequest(service, url, options)
- [ ] parseJsonSafe(text)
- Внедрить в: Telegram sendMessage/sendMediaGroup и VK wall.get/video.get

2) Валидация payload/лицензии/ответов
- [ ] validatePayload(payload, requiredFields)
- [ ] validateLicenseOrThrow(licenseKey)
- [ ] success(data, meta?) / fail(message, code?, meta?) + jsonResponse

3) Caption split централизованно
- [ ] ensureCaptionOrSplit(token, chatId, mediaUrls, caption)
- Сократить sendVkPostToTelegram, убрать дубли

4) Утилиты Sheets (минимум)
- [ ] getRows(name), findRow(name, predicate), appendRow(name, obj)

5) Вынести утилиты в файлы (при желании)
- api-utils.gs, validation-utils.gs, sheets-utils.gs, html-builder.gs, config-manager.gs

### B. Поведение публикации/медиа
6) Публикация реального поста без «теста»
- [ ] Если vk_post не передан в handleSendPost — сервер сам берёт 1 или N неприкреплённых постов из VK по настройке связки

7) Медиа-группы строго одной пачкой
- [ ] Собрать фото в исходном порядке → один sendMediaGroup (до 10)
- [ ] Видео/аудио/доки — отдельным сообщением (список ссылок)

### C. Ссылки и форматирование
8) VK → Telegram Markdown ссылки (НЕ ссылка на пост)
- [ ] [screen_name|Title] → [Title](https://vk.com/screen_name)
- [ ] [club/public/id123|Title] поддержаны
- [ ] [https://example.com|Текст] → [Текст](https://example.com)
- [ ] vk.com/wall-… допускается как отдельный линк, но не подменяет Title-ссылки
- [ ] vk.cc/… оставлять как есть (внешний сайт возможен)
- [ ] Экранировать спецсимволы Markdown в Title
- Тест-кейсы — см. ниже

9) Опции форматирования по флагам
- [ ] Первая строка жирным (on/off)
- [ ] Слова ВЕРХНИМ РЕГИСТРОМ — жирным (on/off)

10) Названия VK/TG — стабильная выдача и кеш
- [x] ✅ getVkGroupName/getTelegramChatName + кеш
- [x] ✅ Информативные логи при ошибках

11) Enhanced Error Logging
- [ ] logApiCall (категории VK_API/TG_API/SYSTEM/USER, http-status, body)
- [ ] Авто-ротация логов

12) Comprehensive Error Handling
- [ ] retryApiCall(backoff)
- [ ] Строгая валидация входных параметров

### D. 🚀 НОВОЕ: Оптимизация производительности
13) Кеширование на сервере
- [x] ✅ PropertiesService кеш для VK/TG названий
- [ ] Батчевые операции для получения названий (handleBatchGetNames)
- [ ] Фоновое обновление кеша по расписанию

14) Оптимизация API запросов
- [ ] Уменьшить таймауты: FAST (8с), MEDIUM (15с), SLOW (30с)
- [ ] Fallback отображение при отсутствии в кеше
- [ ] Ограничение одновременных запросов

---

## 🖥️ CLIENT (client.gs)

1) «Опубликовать последний пост» вместо «Тест»
- [x] ✅ UI: заменить кнопку на «🧪 Тест» (реализовано)
- [ ] Вызов event "send_post" без vk_post → сервер публикует по настройке

2) Первая синхронизация: 1 или N постов
- [ ] В настройке связки: чекбокс «Синхронизировать последние посты», поле N (1..50)
- [ ] Сохранять на сервере (Bindings: sync_recent_enabled, sync_recent_count)
- [ ] В дальнейшем — только новые посты ( > lastKnownId)

3) Published-листы по названию группы
- [ ] Имя листа: Published_<vkGroupName> (без опасных символов)
- [ ] При необходимости — миграция старых листов
- [ ] Шапка: Post ID | Sent At | TG Chat ID | Status | Source
- [ ] Вместо TG Chat ID выводить название чата/канала (tgChatName) при наличии
- [ ] Добавить колонку «Post Preview» — первые 150–300 символов текста поста (без медиа)
- [ ] Формат даты/времени: DD.MM.YYYY, HH:mm (локаль RU)

4) Форматирование — опции в связке
- [x] ✅ Чекбоксы: «Первая строка жирным», «ВСЕ ЗАГЛАВНЫЕ — жирным»
- [x] ✅ Передавать флаги при публикации

5) Массовый переключатель
- [x] ✅ Выключить/включить все связки (кнопка в UI)
- [x] ✅ Серверный endpoint set_global_setting/get_global_setting

6) Активация разрешений ScriptApp (без appsscript.json)
- [x] ✅ Кнопка «⏱️ Авто-проверка» (реализовано)
- [x] ✅ setupTrigger(): понятные сообщения об успехе/ошибке

7) UI имён и статусов
- [x] ✅ В карточке связки — показывать имена VK/TG, URL/chat_id — под подсказкой
- [ ] Warnings, если токены недействительны/бот не в чате

### E. 🚀 НОВОЕ: Оптимизация UI производительности
8) Ускорение операций с связками (КРИТИЧНО - 30сек → 5сек)
- [ ] Двухэтапная загрузка: быстрый UI с fallback → асинхронное обновление названий
- [ ] HTML-side кеширование названий в браузере
- [ ] UI прогресс индикаторы для всех кнопок (⏳, ✅, ❌)
- [ ] Разнесение запросов во времени (setTimeout с задержкой)
- [ ] Убрать блокирующие API вызовы из markPostAsSent()

9) Фоновое обновление названий
- [ ] loadBindingNamesAsync() в HTML с setTimeout
- [ ] Постепенная загрузка названий по одной связке
- [ ] Кеш в localStorage браузера

---

## 🧩 LINK CONVERSION — Правила и тест-кейсы (Server)

Правила:
- [screen_name|Title] → [Title](https://vk.com/screen_name)
- [club123456|Название]/[public123456|Название]/[id123456|Имя] поддержаны
- [https://example.com|Текст] → [Текст](https://example.com)
- vk.com/wall-<id>_<postId> — отдельный линк (не заменяет Title)
- vk.cc/short — оставлять как есть (может вести на внешний сайт)
- Экранировать спецсимволы Markdown в Title

Тесты:
- [durov|Павел Дуров] → [Павел Дуров](https://vk.com/durov)
- [club123456|VK Dev] → [VK Dev](https://vk.com/club123456)
- [daoqub|@daoqub] → [@daoqub](https://vk.com/daoqub)
- [durov|Павел Дуров (VK)] → [Павел Дуров (VK)](https://vk.com/durov)
- https://vk.com/wall-123_456 → остается как есть (standalone ссылка)
- [https://example.com|Сайт] → [Сайт](https://example.com)

---

## 🛠️ CODE SAMPLES (Cheat)

```javascript
// Regex для VK-гиперссылок внутри []
const rx = /\[([^\]|]+)\|([^\]]+)\]/g; // screen_name | Title
text.replace(rx, (m, screen, title) => `[${title}](https://vk.com/${screen})`)

// Безопасная отправка медиагруппы с caption split
if (caption && caption.length > 1024) {
  sendMediaGroupWithoutCaption(token, chatId, mediaUrls);
  sendLongTextMessage(token, chatId, caption);
} else {
  sendMediaGroupWithCaption(token, chatId, mediaUrls, caption);
}

// Первая синхронизация N постов
const count = sync_recent_enabled ? Math.min(sync_recent_count, 50) : 1;

// Кеширование названий (Apps Script)
function getCachedVkGroupName(groupUrl) {
  const cache = PropertiesService.getScriptProperties();
  const cacheKey = `vk_name_${Utilities.computeDigest(Utilities.DigestAlgorithm.MD5, groupUrl, Utilities.Charset.UTF_8)}`;
  
  const cached = cache.getProperty(cacheKey);
  if (cached) {
    const data = JSON.parse(cached);
    if (Date.now() - data.timestamp < 3600000) { // 1 час
      return data.name;
    }
  }
  return groupUrl; // fallback
}

// HTML-side асинхронная загрузка названий
function loadBindingNamesAsync() {
  appState.bindings.forEach((binding, index) => {
    setTimeout(() => {
      google.script.run
        .withSuccessHandler(name => {
          if (name) document.getElementById(`vk-name-${binding.id}`).textContent = name;
        })
        .getCachedVkGroupName(binding.vkGroupUrl);
    }, index * 200); // 200мс между запросами
  });
}
```

---

## 🧹 Чистка репозитория
- Удалить: все разрозненные TODO/README в to_fix, todo_server.gs, todo_clients.gs, todo_clients_permissions.gs, vk_link_markdown_todo.txt (перенесены сюда)
- Истина — этот единый файл: Unified TODO (корень)

---

## 📊 Статус реализации

### ✅ ВЫПОЛНЕНО (CLIENT):
- Тестирование связок (кнопка 🧪)
- Настройки форматирования в связках
- Глобальное отключение магазинов
- Автоматические триггеры
- Показ имен VK/TG групп в UI
- Детальное логирование
- Статистика и админ панель

### ✅ ВЫПОЛНЕНО (SERVER):
- Получение названий VK/TG групп с кешированием
- Глобальные настройки (disable_all_stores)
- Расширенное логирование ошибок
- Валидация токенов при настройке
- Пакетные операции с названиями групп

### 🚧 В РАБОТЕ:
- Оптимизация производительности UI (30сек → 5сек)
- Улучшенная обработка VK → TG ссылок
- Published-листы по названиям групп
- Первая синхронизация N постов

### ⏳ ПЛАНИРУЕТСЯ:
- API рефакторинг и утилиты
- Строгие медиа-группы
- Comprehensive error handling
- Публикация без тестирования