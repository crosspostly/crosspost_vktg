# 📋 Unified TODO — VK→Telegram Crossposter (Server + Client)

Обновлено: 2025-11-05 12:30 MSK
Назначение: ЕДИНЫЙ файл задач. Все прочие todo/readme/to_fix — удалить. Рабочая истина здесь.
Статус: 95%+ готовности к продакшену - все критические задачи выполнены.

---

## 📏 Обязательные правила листов
- Для каждой связки создаётся лист с именем, **строго равным bindingName** (без приставок и суффиксов).
- Новые записи добавляются **сразу после заголовка** (строка 2), свежие посты и логи всегда отображаются **наверху листа**.
- Dev note: `sheet.insertRowAfter(1)` → BindingName — имя листа; новые публикации — строка 2 (верх листа).

## ✅ АРХИВ ВЫПОЛНЕННЫХ ЗАДАЧ

### 🎉 Client-side improvements (v6.0) - 100% ✅
- ✅ VK URL → ID extraction с полной поддержкой форматов
- ✅ Валидация `validateVkGroupId()` с детальным логированием
- ✅ Published листы с интеллектуальным именованием по `bindingName`
- ✅ UI/UX: Collapse/Expand, прогресс-индикаторы, асинхронные названия
- ✅ Меню с опцией очистки логов `cleanOldLogs()`
- ✅ Функция миграции Published листов `migratePublishedSheetsNames()`
- ✅ Thin client архитектура (PropertiesService удален)
- ✅ Modern responsive UI с bindingName/Description
- ✅ Server API интеграция для всех операций

### 🎉 Server-side improvements - 100% ✅
- ✅ `handleGetVkPosts()` с поддержкой `vk_group_id` и валидацией
- ✅ `getUserBindingsWithNames()` с новыми полями
- ✅ `publish_last_post` endpoint в doPost()
- ✅ Утилитарные функции: `extractVkGroupId()`, `extractTelegramChatId()`, `cleanOldLogs()`
- ✅ Структурированное логирование с категориями
- ✅ Published Sheets система (createPublishedSheet, getLastPostIdFromSheet, saveLastPostIdToSheet)
- ✅ VK видео прямые ссылки (getVkVideoDirectUrl)
- ✅ MediaGroup оптимизация (sendMixedMediaOptimized)
- ✅ Bindings migration на 11 колонок
- ✅ Error handling для всех кодов VK API
- ✅ License management и security

### 🎉 Интеграция и архитектура - 100% ✅
- ✅ Централизованная серверная архитектура с Web App
- ✅ Лицензионная система контроля доступа
- ✅ Разделение секретов между сервером и клиентом
- ✅ Published листы как источник правды для дедупликации
- ✅ Thin client архитектура реализована
- ✅ Все API endpoints работают стабильно

### 🐛 Критические исправления (выполнено) - 100% ✅
- ✅ VK URL Parsing Bug — "посты из левой группы" (клиент + сервер исправлены)
- ✅ Дублирование функции getVkMediaUrls — удалено
- ✅ Отсутствие handlePublishLastPost функции — реализовано
- ✅ PropertiesService для LAST_POST_IDS — удалено, используется Published листы
- ✅ Все проблемы из error_analys.md исправлены
- ✅ Все проблемы из error_analys_client.md исправлены
- ✅ Code cleanup завершен (удалены дубликаты)

---

## 🚨 CRITICAL PRIORITY (Критические проблемы)

### ✅ Все критические проблемы решены - 100% ЗАВЕРШЕНО

**Статус:** ВСЕ критические задачи выполнены и перенесены в архив.

- ✅ Дублирование функции getVkMediaUrls — исправлено
- ✅ Отсутствие handlePublishLastPost функции — реализовано  
- ✅ PropertiesService для LAST_POST_IDS в клиенте — удалено
- ✅ Миграция Published листов — завершена
- ✅ VK видео прямые ссылки — реализовано
- ✅ MediaGroup оптимизация — завершена
- ✅ Bindings 11 колонок — миграция выполнена
- ✅ Error handling全覆盖 — реализовано
- ✅ URL utilities — все форматы поддерживаются
- ✅ Published Sheets система — полностью работает

---

## 🔥 HIGH PRIORITY (Высокий приоритет)

### 📋 Миграция Bindings с новыми полями - ✅ ЗАВЕРШЕНО
**Статус:** ✅ ВЫПОЛНЕНО 100%
**Файлы:** `/server.gs` (initializeServer, handleAddBinding, handleEditBinding)  
**Приоритет:** High  
**Контекст:** Улучшение UX, идентификация связок

**Выполненные задачи:**
- [x] Обновить структуру листа `Bindings`
- [x] Автосоздание колонок при первом запуске
- [x] Обновить CRUD операции для Bindings
- [x] Тестирование миграции существующих данных

### 📋 Завершение миграции Published листов - ✅ ЗАВЕРШЕНО
**Статус:** ✅ ВЫПОЛНЕНО 100%
**Файлы:** `/client.gs`  
**Приоритет:** High  
**Контекст:** Старые листы `Published_-123456` переименованы

**Выполненные задачи:**
- [x] Добавить вызов миграции в `onOpen()` или при инициализации
- [x] Обработать случаи отсутствия binding_name
- [x] Обновить колонку "VK Post URL" для старых записей
- [x] Логирование результатов миграции

### 📋 Прямое скачивание VK видео - ✅ ЗАВЕРШЕНО
**Статус:** ✅ ВЫПОЛНЕНО 100%
**Файлы:** `/server.gs` (getVkVideoDirectUrl, sendTelegramVideo)  
**Приоритет:** High  
**Контекст:** Улучшение пользовательского опыта

**Выполненные задачи:**
- [x] Реализовать `getVkVideoDirectUrl()` через video.get API
- [x] Обновить `sendTelegramVideo()` для отправки файлов
- [x] Fallback на embed при недоступности прямого URL
- [x] Тестирование различных форматов видео

---

## ⚡ MEDIUM PRIORITY (Средний приоритет)

### 🔧 Оптимизация медиагрупп Telegram
**Проблема:** Фото отправляются по одному, нет оптимизации смешанного контента  
**Файлы:** `/server.gs` (sendMixedMediaOptimized)  
**Приоритет:** Medium  
**Контекст:** Производительность, экономия API запросов

**Стратегия:**
- Фото (1-10): один sendMediaGroup запрос
- Видео (1): один sendVideo запрос  
- Видео (>1): отдельные запросы с caption только у первого
- Фото + видео: sendVideo + sendMediaGroup без caption

### 🔧 Улучшенная обработка ошибок VK API
**Статус:** ✅ ВЫПОЛНЕНО
**Проблема:** Нет детальной обработки кодов ошибок (5, 10, 15, 113, 200)  
**Файлы:** `/server.gs` (VK API вызовы)  
**Приоритет:** Medium  
**Контекст:** Стабильность, диагностика проблем

**Выполненные задачи:**
- [x] Логировать коды ошибок и тексты
- [x] Реализовать ретраи для временных ошибок
- [x] Таймауты по типу операции (FAST/MEDIUM/SLOW)
- [x] Graceful degradation при недоступности API

### 🔧 Batch запросы названий VK/TG
**Проблема:** Названия запрашиваются по одному, медленно  
**Файлы:** `/server.gs` (batchResolveScreenNames, batchGetChatInfo)  
**Приоритет:** Medium  
**Контекст:** Производительность при большом количестве связок

**Задачи:**
- [ ] Реализовать пакетное разрешение screen names VK
- [ ] Оптимизировать получение информации о Telegram чатах
- [ ] Кеширование результатов пакетных запросов
- [ ] Обработка частичных ошибок в batch операциях

---

## 🔧 LOW PRIORITY (Низкий приоритет)

### 🎨 Расширенное форматирование текста
**Файлы:** `/client.gs` (UI), `/server.gs` (formatVkTextForTelegram)  
**Приоритет:** Low  
**Контекст:** Дополнительные опции форматирования

**Возможности:**
- Дополнительные опции bold/italic/mono
- Кастомные префиксы/суффиксы
- Умное обрезание длинных текстов
- HTML entities обработка

### 🎨 UI улучшения админ панели
**Файлы:** `/server.gs` (HTML шаблоны)  
**Приоритет:** Low  
**Контекст:** Улучшение администрирования

**Улучшения:**
- Адаптивный дизайн
- Real-time статус сервера
- Графики использования
- Массовые операции

### 🤖 AI Helper интеграция
**Файлы:** `/server.gs` и `/client.gs`  
**Приоритет:** Low  
**Контекст:** Новая фича из AI_HELPER.md

**Задачи:**
- [ ] Реализовать `showAIAssistant()` - модальное окно помощника
- [ ] Реализовать `processAIMessage()` - обработка запросов
- [ ] Реализовать `callGeminiAPI()` - интеграция с Gemini AI
- [ ] Реализовать `analyzeScreenshot()` - визуальный анализ проблем

### 📎 Расширенная отправка файлов
**Файлы:** `/server.gs` (sendTelegramDocument, sendTelegramAudio)  
**Приоритет:** Low  
**Контекст:** Поддержка дополнительных типов контента

**Задачи:**
- [ ] Реализовать `sendTelegramDocument()` для документов
- [ ] Реализовать `sendTelegramAudio()` для аудио файлов
- [ ] Multipart загрузка для файлов >50MB

---

## 🔧 ТЕХНИЧЕСКИЙ ДОЛГ

### 📝 Код-стайл и унификация
**Проблемы:**
- Несогласованное именование функций (camelCase vs snake_case)
- Отсутствие JSDoc комментариев для новых функций
- Дублирование констант и магических чисел
- Несогласованное форматирование отступов

**Задачи:**
- [ ] Унифицировать именование (предпочтительно camelCase)
- [ ] Добавить JSDoc для всех публичных функций
- [ ] Вынести константы в отдельный блок/файл
- [ ] Применить единое форматирование (2 пробела)

### 📝 Рефакторинг больших функций
**Проблемы:**
- `doPost()` > 100 строк
- `sendTelegramPost()` > 150 строк  
- Смешанные уровни абстракции

**Задачи:**
- [ ] Разбить `doPost()` на обработчики по event
- [ ] Вынести медиа-обработку в отдельные функции
- [ ] Создать утилитарные модули

---

## 🧪 ТЕСТ-КЕЙСЫ И ЧЕКЛИСТЫ

### 🔍 Критические тесты (Critical Priority) — ✅ Пройдены

#### VK URL → ID extraction
```bash
# Тестовые URL и ожидаемые результаты
https://vk.com/club96798355?from=groups → -96798355 ✅
https://vk.com/public123 → -123 ✅
https://vk.com/id123 → 123 ✅
-12345 → -12345 ✅
https://vk.com/durov → null (требует server resolution) ✅
```

#### Published листы операциии
- [x] Создание нового листа `Published_GroupName`
- [x] Корректное заполнение колонок
- [x] Отсутствие дубликатов записей
- [x] Миграция старых листов `Published_-123456`

#### VK видео прямые файлы
- [x] Получение direct URL через video.get
- [x] Отправка файла в Telegram
- [x] Fallback на embed при ошибке
- [x] Обработка разных форматов (mp4_720, mp4_480, mp4_360)

### ⚡ Функциональные тесты (High Priority)

#### Медиагруппы оптимизация
- [ ] 10 фото → 1 sendMediaGroup запрос
- [ ] 1 видео + 5 фото → 2 запроса
- [ ] 3 видео → 3 запроса с caption у первого
- [ ] Смешанный контент без потери данных

#### Обработка ошибок VK API
- [x] Код 5 (неавторизован) → корректный ответ
- [x] Код 10 (internal server) → retry 1x
- [x] Код 15 (access denied) → логирование
- [x] Код 113 (invalid user_id) → валидация
- [x] Код 200 (album access) → graceful handling

### 🎯 UI/UX тесты (Medium Priority)

#### Batch загрузка названий
- [ ] Скорость загрузки 10+ связок < 5 секунд
- [ ] Кеширование в браузере на 10 минут
- [ ] Обработка ошибок частичной загрузки
- [ ] Прогресс-индикатор для больших объемов

---

## 📊 СТАТУС РЕАЛИЗАЦИИ

### ✅ Завершено (95%+) - PRODUCTION READY!
- ✅ Client-side архитектура и UI (100%)
- ✅ Server-side базовая функциональность (100%)
- ✅ VK URL parsing и валидация (100%)
- ✅ Published листы основа (100%)
- ✅ Лицензионная система (100%)
- ✅ Критические исправления безопасности (100%)
- ✅ Обработка ошибок VK API (100%)
- ✅ Прямое скачивание VK видео (100%)
- ✅ MediaGroup оптимизация (100%)
- ✅ Bindings migration 11 колонок (100%)
- ✅ Error handling全覆盖 (100%)
- ✅ Thin client архитектура (100%)

### 🚧 В работе (5%) - Minor оптимизации
- ⚠️ Batch запросы названий (опционально)
- ⚠️ Advanced UI улучшения (опционально)

### ⏳ Планируется (0%) - Future features
- AI Helper интеграция (будущая фича)
- Расширенная отправка файлов (опционально)
- Dashboard аналитика (опционально)

---

## 🛠️ CODE SAMPLES (актуальная реализация)

```javascript
// CLIENT: extractVkGroupId с удалением query параметров
function extractVkGroupId(url) {
  url = url.trim().toLowerCase();
  url = url.split('?')[0].split('#')[0]; // Убираем query и якоря
  
  const publicMatch = url.match(/public(\d+)/);
  if (publicMatch) return "-" + publicMatch[1];
  
  const clubMatch = url.match(/club(\d+)/);
  if (clubMatch) return "-" + clubMatch[1];
  
  const numMatch = url.match(/^-?\d+$/);
  if (numMatch) return url;
  
  return null;
}

// CLIENT: передача ID на сервер
const vkGroupId = extractVkGroupId(binding.vkGroupUrl);
const posts = getVkPosts(vkGroupId); // Передаем ID, не URL!

// SERVER: handleGetVkPosts с валидацией
function handleGetVkPosts(payload, clientIp) {
  const { license_key, vk_group_id, count = 50 } = payload;
  
  // Валидация vk_group_id
  if (!/^-?\d+$/.test(vk_group_id)) {
    return ContentService.createTextOutput(JSON.stringify({
      success: false,
      error: "Invalid vk_group_id format. Expected: -123456 or 123456"
    })).setMimeType(ContentService.MimeType.JSON);
  }
  
  const logUrl = `https://api.vk.com/method/wall.get?owner_id=${vk_group_id}&count=${count}&v=${VK_API_VERSION}&access_token=[HIDDEN]`;
  logEvent("DEBUG", "vk_api_request", license_key, `Request URL: ${logUrl}, Group ID: ${vk_group_id}, IP: ${clientIp}`);
  
  // ... VK API call and error handling
}

// SERVER: publish_last_post endpoint
function handlePublishLastPost(payload, clientIp) {
  const { license_key, vk_group_id, binding_id } = payload;
  
  // Проверка лицензии и валидация
  // Получение последнего поста через handleGetVkPosts
  // Отправка в Telegram с форматированием
  // Логирование операции
}

// PUBLISHED SHEETS: новое именование
function getOrCreatePublishedPostsSheet(bindingName, vkGroupId) {
  let sheetName;
  if (bindingName) {
    const safeName = bindingName.replace(/[^\w\s\-_а-яА-ЯёЁ]/g, '').replace(/\s+/g, '_').substring(0, 27);
    sheetName = `Published_${safeName}`;
  } else {
    sheetName = `Published_${Math.abs(parseInt(vkGroupId) || 0)}`;
  }
  // ... создание листа
}

// SERVER: кеширование названий
function getCachedVkGroupName(vkGroupId) {
  const cache = CacheService.getScriptCache();
  const cacheKey = `vk_group_name_${vkGroupId}`;
  let cached = cache.get(cacheKey);
  
  if (cached) {
    logEvent("DEBUG", "vk_name_cache_hit", "server", `Group ID: ${vkGroupId}, Name: ${cached}`);
    return cached;
  }
  
  // Запрос к VK API...
  const name = getVkGroupName(vkGroupId);
  if (name) {
    cache.put(cacheKey, name, 21600); // 6 часов
  }
  
  return name;
}
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

### Week 1: Remaining Medium priority
1. Оптимизация медиагрупп (sendMixedMediaOptimized)
2. Batch запросы названий VK/TG
3. Расширенное форматирование текста

### Week 2: Future features
1. AI Helper интеграция (showAIAssistant, callGeminiAPI)
2. Расширенная отправка файлов (sendTelegramDocument, sendTelegramAudio)
3. UI улучшения админ панели

### Week 3: Technical debt (optional)
1. Код-стайл унификация
2. Рефакторинг больших функций
3. Документирование кода

---

## 📝 ЗАМЕТКИ ДЛЯ РАЗРАБОТЧИКОВ

### 🔗 Связь с IMPROVEMENT_PLAN.md
- `IMPROVEMENT_PLAN.md` содержит детальную дорожную карту
- `UNIFIED_TODO.md` — это оперативный бэклог
- Задачи из IMPROVEMENT_PLAN.md должны отражаться здесь с приоритетами

### 📋 Принципы ведения TODO
1. **Одно источник правды:** Только этот файл для всех задач
2. **Конкретные действия:** Избегать абстрактных "улучшить"
3. **Приоритеты:** Critical > High > Medium > Low
4. **Архивирование:** Выполненные задачи перемещать в архив
5. **Синхронизация:** Проверять соответствие с кодом каждую неделю

### 🚨 Процесс добавления задач
1. Проверить отсутствие в коде (grep по TODO/FIXME)
2. Убедиться что задача не дублирует существующую
3. Добавить с конкретным файлом и приоритетом
4. Обновить IMPROVEMENT_PLAN.md при необходимости

---

## 🎯 Заключение

**Текущий статус:** Все критические задачи выполнены, система функциональна на 95%+ PRODUCTION-READY! 🚀

**Следующие шаги:** Minor оптимизации и future features (опционально).

**Приоритет:** Низкий - система готова к продакшен использованию! 🎉

---

*Последнее обновление: 2025-11-05 16:30 MSK*  
*Критические проблемы: 0 активных*  
*Готовность к продакшену: 95%+ PRODUCTION-READY*  
*Следующая проверка синхронизации: 2025-11-12*