/**
 * TODO_CLIENTS.GS — план доработок client.gs и связанных серверных правок
 * Дата: 2025-11-02
 * Назначение: синхронизация ожиданий по UX и поведению клиента с сервером
 */

// 1) Публикация реального поста вместо "теста"
//   КЛИЕНТ:
//   - Заменить кнопку "🧪 Тест" на "▶️ Опубликовать последний пост"
//   - Вызовать server: event "send_post" без vk_post → сервер сам берет нужный пост по настройкам связки
//   СЕРВЕР:
//   - В handleSendPost, если vk_post отсутствует: взять последний (или N) неприкрепленных постов из VK и отправить в TG

// 2) Синхронизация последних N постов при первом запуске
//   КЛИЕНТ:
//   - В настройке связки добавить чекбокс "Синхронизировать последние посты" и числовое поле N (1..50)
//   - Отправлять эти поля при addBinding/editBinding
//   СЕРВЕР:
//   - Сохранить sync_recent_enabled, sync_recent_count в Bindings
//   - При пустом vk_post публиковать 1 пост или N постов согласно настройке

// 3) Published-листы должны называться по названию сообщества VK, а не по ID
//   КЛИЕНТ:
//   - При создании листа Published_ использовать getUserBindingsWithNames → vkGroupName
//   - Имя листа: Published_<vkGroupName> (без спец. символов, ограничение Sheets ~100 символов)
//   - Если имя поменялось, мигрировать старый лист (опционально) или создать новый
//   СЕРВЕР:
//   - Убедиться, что getVkGroupName стабильно возвращает имя; кеш в Script Properties

// 4) Медиа отправлять одной медиагруппой (в исходном порядке), а не отдельными сообщениями
//   КЛИЕНТ:
//   - Ничего не делать (серверная задача)
//   СЕРВЕР:
//   - Гарантировать сбор всех фото из attachments в массив mediaUrls в исходном порядке
//   - Использовать sendMediaGroupWithCaption/WithoutCaption; не дублировать отправку картинок поштучно
//   - Видео/аудио/доки — второй пост со списком ссылок (как сейчас), но фото строго в одном media group (до 10)

// 5) Удалить колонку "Client Version" из Published_* листов
//   КЛИЕНТ:
//   - При создании шапки исключить "Client Version"
//   - В markPostAsSent писать: Post ID | Sent At | TG Chat ID | Status | Source
//   СЕРВЕР:
//   - Не зависит, чисто клиентская таблица

// 6) Опции форматирования
//   КЛИЕНТ:
//   - В карточке связки добавить чекбоксы: "Первая строка жирным", "Слова ВЕРХНИМ РЕГИСТРОМ — жирным"
//   - Сохранять на сервере и передавать при публикации
//   СЕРВЕР:
//   - Применять опции в formatVkTextForTelegram условно

// 7) Массовый переключатель "Выключить все магазины"
//   КЛИЕНТ:
//   - Чекбокс/кнопка над списком связок: Выключить/Включить все
//   - По умолчанию: пройтись по всем связкам и вызвать toggleBindingStatus
//   СЕРВЕР (улучшение):
//   - Добавить метод toggle_all_bindings (по license_key + new_status) для одной операции

// 8) Правильное форматирование ссылок VK → Telegram Markdown
//   КЛИЕНТ:
//   - Не вмешиваться в текст; форматирование на сервере
//   СЕРВЕР:
//   - Улучшить регулярку: [screen_name|Title] → [Title](https://vk.com/screen_name)
//   - Не дублировать концы вида "Title (https://vk.com/...)"

// 9) Исправить получение названий групп VK и чатов TG
//   КЛИЕНТ:
//   - Основной источник — server.get_user_bindings_with_names → отображать vkGroupName/tgChatName
//   СЕРВЕР:
//   - Проверить VK_USER_ACCESS_TOKEN и права бота в TG; кеширование имен; информативные логи

// 10) UX/табличные улучшения
//   КЛИЕНТ:
//   - В Published_* хранить только необходимое; убрать Client Version
//   - В карточке связки показывать имена, а URL/chat_id — под спойлер/tooltip
